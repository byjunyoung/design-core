{
  "name": "design-core",
  "version": "0.0.1",
  "description": "A design tool for product screens where the agent holds the pen. Engine: screen files, lint, states, flows.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "bin": { "design-core": "./src/cli.js" },
  "exports": { ".": "./src/index.js" },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "lint:examples": "node src/cli.js lint examples/orders --branch feature/demo"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.6.0"
  }
}
